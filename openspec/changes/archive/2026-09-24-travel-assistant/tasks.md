## 1. 项目骨架

- [x] 1.1 在仓库根目录建立 `frontend/`（Vue 3 + TypeScript + Vite + Element Plus）和 `backend/`（FastAPI + uv），并确认两边的启动命令能在本地跑起来
- [x] 1.2 添加 `docker-compose.yml`、`.env.example` 和 `.gitignore`，变量名与 design.md 一致且不含真实密钥；执行 `docker compose config` 确认文件合法
- [x] 1.3 用 Compose 启动 MySQL 和 Redis，确认后端能连上这两个服务

## 2. 用户、会话和数据表

- [x] 2.1 创建用户、会话、对话、消息、行程、偏好、trace、span、token 用量等表，并用一次迁移或启动建表验证表都存在
- [x] 2.2 实现登录、退出和当前用户接口；未登录访问对话接口返回拒绝，用接口测试验证
- [x] 2.3 用户表为空时用环境变量创建管理员；重复启动不会再创建第二个管理员，用启动两次验证

## 3. 外部工具

- [x] 3.1 实现高德天气、地理编码、POI 和路径调用，超时默认 8 秒；用成功响应和超时各写一条测试
- [x] 3.2 实现 Tavily 搜索，最多返回 5 条标题、链接和短摘要，不返回正文；用一条夹具响应验证条数和字段
- [x] 3.3 工具失败时返回明确错误并继续流程；用全部失败和部分失败两种夹具验证调用方仍能拿到已成功的材料

## 4. 模型网关和熔断

- [x] 4.1 实现 `call_model`，按环境变量调用主模型 `qwen3.7-plus` 和 DeepSeek 的 OpenAI 兼容接口；用假客户端验证关闭状态下走主模型
- [x] 4.2 实现 Redis 上的关闭、打开、半开熔断器，默认 60 秒内 5 次失败后打开、冷却 30 秒；用单元测试覆盖阈值打开、4xx 不计数、半开只有一个试探、试探成功关闭、试探失败重新打开
- [x] 4.3 首 token 之前失败时改走备用模型；已经输出 token 后失败则结束本轮且不拼接另一模型的文本；用假流式响应验证这两种情况

## 5. 记忆和追踪

- [x] 5.1 实现短期上下文（偏好 + 滚动摘要 + 最近 6 轮）和长期偏好的查看与修改；用测试验证新对话不带入其他对话原文，且修改后的偏好会被读到
- [x] 5.2 上下文估算超过 6000 token 时压缩较早消息，工具结果计入预算；用一条超长对话验证下次上下文含摘要且不含全部早期原文
- [x] 5.3 回复返回后异步抽取偏好，抽取失败不改变已返回的回复；用测试验证响应先完成
- [x] 5.4 为每轮分配 `trace_id`，写入记忆读取、模型调用、工具调用和压缩 span，token 使用供应商 usage；用一轮含工具调用的夹具验证 span 和用量条数

## 6. 行程图和对话接口

- [x] 6.1 用 LangGraph 建 extract、clarify、build、answer 三路图，节点为普通函数；用夹具验证缺目的地或非法天数时只追问且不调用外部工具
- [x] 6.2 实现 build：新行程按天气、搜索、POI、写行程的顺序执行；目的地和日期不变的修改跳过前三步并返回完整新行程；用夹具验证调用次数
- [x] 6.3 实现 answer：最多一次外部查询；用「怎么走」和「行程里已有答案」两条夹具验证查询次数
- [x] 6.4 实现对话的创建、列表、详情和发送消息的 SSE（进度、正文、行程 JSON、`trace_id`）；用接口测试验证用户只能访问自己的对话，且响应带 `trace_id`

## 7. 前端

- [x] 7.1 实现登录页和对话页：新对话、消息流、Markdown、行程卡片、工具进度；在浏览器里走通「缺信息追问」和「生成多日行程」
- [x] 7.2 实现偏好页的查看和保存；修改预算后新建对话，确认后续请求会带上新偏好
- [x] 7.3 实现管理员的 trace 和用量页；普通用户看不到入口，直接请求接口被拒绝；管理员能按 `trace_id` 看到步骤并用时间范围看到汇总

## 8. 整体验收

- [x] 8.1 执行 `docker compose up --build`，用浏览器完成登录、追问、生成行程、修改行程和管理员查看 trace；确认仓库内没有真实密钥
